# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'
use_frameworks!

target 'IMDBTop' do
  # Pods for IMDBTop
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'Moya',       '~> 12.0'

end

target 'IMDBTopTests' do
  inherit! :search_paths
  # Pods for testing
  pod 'RxBlocking', '~> 4.0'
  pod 'RxTest',     '~> 4.0'

end
